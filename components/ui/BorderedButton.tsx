const BorderedButton = ({ children }: { children: React.ReactNode }) => {
  return;
  <button className='focus:shadow-outline my-auto h-8 rounded-md border border-icon-purple px-5 font-medium text-icon-purple transition-colors duration-150 hover:bg-fade-purple '>
    Join
  </button>;
};

export default BorderedButton;
